# Hydir product plan: a Ghidra-backed lifting and reverse-engineering framework

Status: active direction, updated 2026-09-26. This is the current product and implementation plan. The earlier [CTF workflow proposal](HYDIR_CTF_WORKFLOW_PLAN.md) is retained as historical research; its release priorities are superseded here.

## What Hydir is

**Hydir is a binary lifting and reverse-engineering framework with its own GUI and Rust analysis core.** A user opens a binary in Hydir. Hydir starts a version-pinned, containerized Ghidra headless worker, imports and analyzes the binary there, extracts P-code and other facts, then populates Hydir's GUI and artifacts. Hydir converts P-code into its own explicit semantic IR in Rust, runs inspectable analyses and checked transformations, and exports useful LLVM IR and other results with links to the source instructions.

Hydir's existing desktop GUI is the primary user experience. The CLI, Python SDK, and API invoke the same pipeline. The Ghidra project is a managed internal analysis cache; an expert may also import an existing project or use Ghidra directly. Analyst corrections happen in Hydir's model and GUI. CTF challenges are useful fixtures and demos for the framework.

The first competitive reason to use Hydir should be concrete: **open one binary, get a reproducible address-linked lift automatically, run a custom analysis or transformation, and see exactly which semantics and facts support the result.** We should measure this workflow before claiming an advantage over Ghidra, angr, Miasm, rev.ng, or another tool.

## Where the code stands today

| Existing component | Reuse in the new direction | Missing contract |
| --- | --- | --- |
| Native x86-64 ELF loader, discovery, MachineIR, StateIR, FunctionIR, CIR, model, C output | Keep as a native frontend, semantic reference, and regression corpus | It does not ingest Ghidra P-code or project facts |
| `integrations/ghidra/HydIRExport.java` | Reuse its function/CFG/call-graph extraction in a new snapshot exporter; preserve its v1 JSON reader | It exports function/CFG/call-graph facts and a program name, but no P-code, types, bytes, or durable project identity |
| `integrations/ghidra/HydIR` extension | Later entry point and address navigation | It currently calls Hydir's remote decompile/patch commands; it is not a project/P-code importer |
| Desktop GUI, including its Ghidra graph view | Make this the entry point that launches analysis and presents linked results | It displays external graph JSON but cannot orchestrate Ghidra or lift P-code yet |
| LLVM exporter | Reuse its validation and artifact plumbing | Its current native form contains effect descriptors and does not establish executable equivalence |
| AnalysisModel, typed C, cache, API/SDK, VM explorer, capture/solver work | Build on these after the importer works | None makes Ghidra the primary frontend yet |

**Status claim (2026-09-26):** a thin headless Ghidra exporter and Rust importer produce a binary-bound raw P-code artifact with analyzed flow and call evidence from a real x86-64 ELF. The CLI launches a bounded worker automatically, and the GUI opens an ELF, imports the function index, and browses source-linked raw P-code, state effects, flow, calls, and LLVM fragments. The default worker has a pinned Docker image definition; its container route is untested on this host because Docker is unavailable. A local Ghidra 12.1.4 route has passed an end-to-end fixture and cache check, including reuse of a saved analyzed project for another selected function. Rust classifies a limited pure integer P-code subset, emits an ordered state-effect inventory and an incomplete instruction CFG artifact, concretely executes an exact listed prefix, and executes bounded RAM loads and stores when addresses and bytes are known. A separate bounded concrete path trace follows validated P-code branches and analyzed fallthrough on seeded state, with explicit stop reasons and source provenance. The CLI and local Python SDK expose binary-bound seed files for prefix and path traces. Hydir emits verifier-clean LLVM for an individual operation or bounded prefix, plus a runnable standalone LLVM byte-state module for that prefix with a deterministic register/unique alias map. An `lli` comparison matches a selected state byte against Hydir's concrete executor on the PRISM fixture. Executable whole-function state/CFG semantics, full-function LLVM, remote API exposure of this new route, and the later analysis and rewrite gates remain open. The previous estimate that roughly 60% of the *typed native decompilation* milestone was done does not measure progress on this revised product.

## Architecture and artifact contract

```text
Binary opened in Hydir GUI (or passed to CLI/SDK/API)
  -> Hydir job orchestrator -> pinned Ghidra headless container
     -> managed project + auto-analysis
  -> bounded, versioned project snapshot
     raw P-code + instruction bytes + address spaces + flow overrides
     CFG/calls + symbols + prototypes/types + analyst changes
  -> Rust snapshot validation -> PcodeIR -> explicit StateIR -> analyses/transforms
  -> Rust LLVM emission / analysis artifacts / optional C
  -> Hydir desktop GUI + CLI/SDK/API
  -> links back to source instructions in Hydir

Native ELF frontend -> existing MachineIR/StateIR path (retained)
```

1. **Ghidra snapshot contract.** The current schema v2 exports a program/function index and one selected function's bounded raw P-code, instruction bytes, address spaces, Ghidra identity, and original binary SHA-256. The next revision adds managed project identity, memory blocks, CFG edges, calls, symbols, prototypes, type facts, and analyst changes; it must bump the schema when adding required fields. Headless Ghidra will create a managed project from the binary, and the same exporter can read an existing expert-supplied project. Use stable IDs based on address spaces and addresses, not display names. Validate digests, ranges, sizes, references, edges, and schema version on import. Never silently merge snapshots from different binaries.
2. **Separate raw and high P-code.** Raw instruction P-code is the semantic input. Export decompiler/high P-code in an optional second section for SSA, inferred variables, and type hints; keep it tagged as Ghidra's analysis and retain op/address provenance. Raw and high P-code can differ, including overrides and decompiler-created operations. A high-P-code type is evidence, not proof of raw-machine semantics.
3. **PcodeIR v1.** Preserve operation ordering and unique temporaries, register aliasing, bit widths, endian and address-space rules, `LOAD`/`STORE` space IDs, control-flow destinations, `CALLOTHER` userops, and unknown or injected effects. Lower to Hydir's explicit state/effect layer through a specified opcode table. The first lift targets x86-64; architecture-neutral state concepts must be separated from x86-specific registers before adding other Ghidra languages. Unsupported operations stay explicit and block exactness claims.
4. **LLVM export.** Emit verifier-clean LLVM for the supported P-code subset with a documented state and memory ABI. Use memory intrinsics or typed helper calls where ordinary LLVM loads/stores would erase guest-memory distinctions. Export a manifest of translated, opaque, and assumed effects; a module with opaque hooks is inspectable but not standalone equivalent. Do not feed optimized LLVM back into a rewrite path until its preconditions are checked.
5. **Analysis layer.** Expose graph traversal, def-use, slicing, type and alias evidence, symbolic/concrete evaluation, and transformation passes over stable artifact APIs. Cache by binary digest, Ghidra snapshot revision/fingerprint, Hydir analysis version, and options. A project edit invalidates the affected function and dependent summaries. Keep provenance and unresolved facts visible in API results.
6. **Integration.** Opening a binary in the existing Hydir GUI starts or reuses the container job, shows progress and errors, imports the index, and links functions, graph, raw P-code, state IR, LLVM, provenance, and diagnostics. `hydirctl analyze <binary>` and equivalent SDK/API calls invoke the same orchestrator. Direct snapshot import remains available for advanced users and tests. Exact command spelling can change before release; the versioned snapshot contract cannot change silently. The Ghidra extension is an optional expert integration.

## Implementation order and gates

| Order | Deliverable | Acceptance gate |
| --- | --- | --- |
| 0. Contract | Extend the checked-in Ghidra snapshot v2 and PcodeIR v1 fixture into the complete project contract | Deterministic round-trip; invalid digests, widths, varnodes, edges, and schema versions fail clearly |
| 1. Project bridge | Add a v2 exporter beside `HydIRExport.java` to capture the program index and a selected function's raw P-code, bytes, CFG, calls, and project metadata; import it in Rust | Headless import and export of a binary succeeds; every P-code op has an address and order; existing v1 graph JSON still loads |
| 1a. Automatic Ghidra worker | Package a pinned Ghidra release, required JDK, and exporter in a container; have Hydir provision, launch, and monitor it | On a prepared demo machine, opening a binary in Hydir requires no Ghidra or container commands and populates the function index; cancellation, timeout, cache reuse, and failure diagnostics work |
| 2. Semantic lift | Lower core integer, bitwise, comparison, extension, branch, memory, and call operations into explicit state/effects | Per-op and per-function tests at multiple optimization levels; unsupported operations appear as opaque effects |
| 3. LLVM | Implement the supported P-code subset in Rust through Hydir PcodeIR and StateIR to LLVM; compare Patchestry outputs on the same fixtures as an external reference only | `opt -verify` plus behavioral comparison against original function and a Ghidra P-code oracle on exact fixtures; failed comparison removes the exact claim |
| 4. Workbench and framework API | Hydir GUI plus stable CLI, Python, and service access to snapshot, IR, provenance, diagnostics, and pass outputs | A user opens a binary and inspects linked results in Hydir GUI; an external script runs the same analysis and maps results to source addresses |
| 5. Analyses | Incremental project updates, interprocedural summaries, type constraints, transformation preconditions, VM-aware views | Changed-function and caller invalidation tests; ambiguous aliases stay unresolved; checked transforms survive differential tests |
| 6. Breadth | More Ghidra languages, whole-program import, richer decompilation and UI, VM recovery | Add each target only with a project fixture, semantic oracle, coverage report, and explicit unsupported cases |

**First end-to-end demo:** start Hydir and open a stripped x86-64 ELF. Hydir automatically runs containerized Ghidra analysis and fills the function list. Select a function and inspect its linked raw P-code, state IR, LLVM, slice, and source instructions inside Hydir. Repeat the analysis through the CLI/SDK. Show one unsupported instruction whose effect remains visible. The presenter does not launch or operate Ghidra. CTF binaries can stress the same path later.

The test matrix starts with straight-line arithmetic, branches, loops, calls, stack/global memory, indirect flow, and at least one `CALLOTHER` case. Use stripped and DWARF-bearing builds at several optimization levels. Record coverage by opcode and artifact fidelity. LLVM verification alone is never a semantic test.

### Reuse decisions from the 2026-09-26 research pass

| Gap | Adopt or adapt | Boundary and reason |
| --- | --- | --- |
| Binary import, auto-analysis, CFG, symbols, types, raw P-code | Use Ghidra's [Headless Analyzer](https://github.com/NationalSecurityAgency/ghidra/blob/master/Ghidra/RuntimeScripts/support/analyzeHeadlessREADME.md), [instruction P-code API](https://ghidra.re/ghidra_docs/api/ghidra/program/model/listing/Instruction.html), and optional [Decompiler API](https://github.com/NationalSecurityAgency/ghidra/blob/Ghidra_12.1.4_build/Ghidra/Features/Decompiler/src/main/java/ghidra/app/decompiler/DecompInterface.java) | Build a v2 Java exporter beside Hydir's graph exporter. Headless Ghidra already creates projects, analyzes files, and runs post-analysis scripts. Raw instruction P-code and decompiler P-code remain separate. |
| Headless container and rich Ghidra JSON | Study [Patchestry's exporter](https://github.com/lifting-bits/patchestry/blob/main/scripts/ghidra/PatchestryDecompileFunctions.java), [serializer](https://github.com/lifting-bits/patchestry/blob/main/scripts/ghidra/util/PcodeSerializer.java), and [container](https://github.com/lifting-bits/patchestry/blob/main/scripts/ghidra/decompile-headless.dockerfile); compare its output on a Hydir fixture | Patchestry uses high P-code and synthetic operations, so its JSON is a comparison source, not Hydir's semantic input or runtime dependency. Hydir's thin Java exporter reads Ghidra APIs and writes the snapshot; Rust owns parsing, validation, models, lifting, analysis, and LLVM emission. Keep Hydir's snapshot schema and avoid Patchestry's patched LLVM/MLIR toolchain. |
| P-code execution oracle | Use [Ghidra's P-code emulator](https://github.com/NationalSecurityAgency/ghidra/blob/master/GhidraDocs/GhidraClass/Debugger/B2-Emulation.md) in bounded tests; evaluate [Icicle](https://github.com/icicle-emu/icicle-emu) and [angr's P-code engine](https://docs.angr.io/en/v9.2.223/api/angr.engines.pcode.html) as independent cross-checks | Reuse existing engines for semantics tests. Ghidra documents cases where P-code designed for decompilation is insufficient for emulation, so native execution remains a separate oracle for exact x86-64 claims. |
| Alternate SLEIGH/P-code libraries | Evaluate [pypcode](https://docs.angr.io/projects/pypcode/en/v3.0.1/) and [Fugue](https://github.com/fugue-re/fugue-core) for tests and utility types | They can lift bytes or model IR without a Ghidra project. Hydir needs Ghidra's analyzed program facts and already has a versioned IR, so replacing the frontend or IR adds work without closing the first product gap. |
| LLVM translation | Read [Ghidrall](https://github.com/toor-de-force/Ghidrall), [Ghidra-to-LLVM](https://github.com/toor-de-force/Ghidra-to-LLVM), [Patchestry](https://github.com/lifting-bits/patchestry), and [Remill](https://github.com/lifting-bits/remill) for opcode and memory-model choices | Compare semantics, coverage, and test fixtures with these projects. Implement the translation in Hydir's Rust core using its state/effect and fidelity contracts; no external translation backend is part of the product path. |
| Recompilation and patching | Keep Hydir's existing restricted patch/rebuild routes; compare [rev.ng's recompile artifacts](https://docs.rev.ng/references/artifacts/) and Patchestry's contracts | Recompilation from lifted IR already exists elsewhere. Hydir should demonstrate one checked function rewrite with an explicit scope and native replay before promising arbitrary binary round trips. |
| Binary parsing and data types | Keep the existing `object`/`gimli` loader and AnalysisModel; use Ghidra's project facts as another evidence source | [Goblin](https://github.com/m4b/goblin) solves parsing, not P-code translation. Replace the loader only for a measured gap. |

**Provisional worker pin:** the official [Ghidra 12.1.4 release](https://github.com/NationalSecurityAgency/ghidra/releases/tag/Ghidra_12.1.4_build) published SHA-256 `ddac49f903da9d5bac833e5cc79395098b9c33cfd3279be5f31bd00387d2d4db` for its release archive; its [Getting Started guide](https://github.com/NationalSecurityAgency/ghidra/blob/Ghidra_12.1.4_build/GhidraDocs/GettingStarted.md) requires JDK 21. Resolve the exact archive name and verify this digest in the image build. The existing Hydir extension targets 12.1.3 and needs a separate compatibility check; the first worker runs a Java script and does not require that extension. The current development host has no Docker executable, so the worker image and end-to-end route are not yet verified.

The [Patchestry image definition](https://github.com/lifting-bits/patchestry/blob/main/scripts/ghidra/decompile-headless.dockerfile) is a useful build example, but it pins Ghidra 12.0.4, grants passwordless sudo to its runtime user, and removes Ghidra's `licenses` directory. Hydir's image should use its own validated release pin, keep notices, and run without elevated privileges.

**Implementation slices:**

1. Export a deterministic program index and one raw-P-code function snapshot from a new, thin `HydIRSnapshot.java` beside the existing v1 graph exporter, using the pinned Ghidra release. Compare Patchestry's JSON on the same fixture to identify useful facts, then define Hydir's own snapshot and Rust importer with raw/high distinction and provenance. Check in the fixture and an invalid-snapshot set.
2. Add a Rust snapshot parser/validator and `PcodeIR v1` artifact. Display the imported function, operations, source addresses, and unsupported-effect diagnostics in Hydir's GUI. This gate works from a saved snapshot before container orchestration is added.
3. Package and launch the headless worker from a shared Hydir job path. The GUI already invokes `hydirctl` for some external tasks, so the first UI integration can call a new CLI analysis command. Opening a binary automatically creates/reuses a managed project, imports the function index, and requests selected functions on demand. Cache by binary digest plus exact Ghidra/exporter/analysis settings; surface progress, cancellation, timeout, and failures in the GUI. The SDK and service use the same job path.
4. Implement a documented x86-64 P-code subset in Rust through Hydir's state IR to LLVM. Run side-by-side comparisons with Patchestry on `hydir_max2`, a PRISM branch, and one memory/call fixture; record opcode coverage, address provenance, executable LLVM behavior, and missing effects. Compare bounded execution against Ghidra P-code evaluation and native ELF runs, show an exact/partial ledger, and add one meaningful Rust pass and checked rewrite only after comparisons pass.

The exporter, Rust P-code and ordered state artifacts, automatic CLI worker, durable analyzed Ghidra project, and GUI raw P-code view are in place. The current CLI command is `hydirctl ghidra analyze`. Snapshot persistence in `hydir-project` and executable function-level semantic/LLVM lowering are the next ownership steps. Preserve `HydIRExport.java` and the v1 graph reader. Continue with `hydir_max2` and PRISM ELF fixtures before adding new binaries.

**Demo go/no-go:** from a prepared installation, one user action opens an unfamiliar stripped ELF in Hydir; the Ghidra worker populates functions; selecting a function gives linked disassembly, raw P-code, Hydir IR, LLVM, and an analysis result; an unsupported effect is visible; one supported transformed function passes native replay. If any stage requires operating Ghidra or a container manually, or if LLVM syntax verification is presented as behavior proof, the demo gate fails.

### Automatic container contract

Hydir's default Ghidra-backed analysis path launches a reproducible **headless Ghidra worker** automatically. Pin the official Ghidra release and JDK, verify the release checksum during the image build, retain bundled license notices, and record exact versions in each snapshot. Run as a non-root user with bounded CPU, memory, and time; default to no network, a read-only binary mount, writable scratch project space, and a mounted output directory. Hydir checks or provisions the pinned image and reports missing runtime setup or analysis failures inside its GUI; users never need Ghidra or container commands for the normal flow. Keep the native ELF path available. For expert-supplied projects, process an isolated copy after it is closed in the Ghidra GUI. Container packaging follows the snapshot contract so it can be tested against a real exporter. The demo host may have its runtime preinstalled; a public installer needs an in-app runtime setup path.

## Design references and limits

- Ghidra documents [instruction P-code](https://ghidra.re/ghidra_docs/api/ghidra/program/model/listing/Instruction.html), [P-code operations](https://ghidra.re/ghidra_docs/api/ghidra/program/model/pcode/PcodeOp.html), and [headless project analysis](https://github.com/NationalSecurityAgency/ghidra/blob/master/Ghidra/RuntimeScripts/support/analyzeHeadlessREADME.md). Headless analysis can process an existing project or import a new binary; Ghidra warns that a project already open in its GUI may not run headlessly.
- Ghidra's [NOTICE](https://github.com/NationalSecurityAgency/ghidra/blob/master/NOTICE) describes its Apache 2.0 license and bundled third-party components. Container redistribution must carry the corresponding notices.
- [Ghidrall](https://github.com/toor-de-force/Ghidrall) demonstrates P-code-to-LLVM translation and is a research reference. Its coverage and validation are not Hydir's correctness contract.
- [Remill's explicit memory intrinsics](https://github.com/lifting-bits/remill/blob/master/docs/INTRINSICS.md) and [Anvill's specification-driven lift](https://github.com/lifting-bits/anvill) are useful design comparisons. Hydir still needs its own import, semantics, and tests.
- [Goblin](https://github.com/m4b/goblin) parses object formats; it does not translate P-code. The existing `object`/`gimli` loader is adequate until a measured loader gap appears.

## Product decisions

- Opening a binary in Hydir is the default front door. Hydir manages Ghidra headlessly and imports its project/P-code facts. The native x86-64 ELF path remains supported and can cross-check results.
- Hydir's versioned IR remains canonical inside the framework. LLVM is an export and analysis interchange, not an implicit replacement for the IR or a proof of equivalence.
- Hydir implements snapshot parsing, P-code and state semantics, analyses, LLVM emission, transformations, and patch/rebuild logic in Rust. The Ghidra container runs a thin Java exporter; Patchestry is a research and comparison reference, not a runtime or translation backend.
- Raw P-code is the semantics baseline. High P-code, prototypes, types, and analyst edits are valuable project evidence with separate provenance.
- Hydir's existing desktop GUI is the primary workbench and owns the automatic analysis flow. CLI, SDK, and API access expose the same framework; the Ghidra extension supports expert project exchange.
- No broad architecture, decompiler-quality, VM, solve-rate, or competitive superiority claim ships without a task-specific gate.
